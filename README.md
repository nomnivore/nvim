# nomnivore's neovim config

This is my personal neovim config, which extends and builds upon the LazyVim config by folke.

---

Update plugins from command line:

```bash
nvim --headless "+Lazy! sync" +qa
```
